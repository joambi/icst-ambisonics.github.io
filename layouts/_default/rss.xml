{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}

{{- $pages := slice -}}
{{- if or .IsHome .IsSection -}}
  {{- $pages = $pctx.RegularPages -}}
{{- else -}}
  {{- $pages = $pctx.Pages -}}
{{- end -}}

{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = first $limit $pages -}}
{{- end -}}

{{- /* Author fallback logic (Hugo 0.156 safe) */ -}}
{{- $aemail := (or .Site.Params.author.email .Site.Params.email) -}}
{{- $aname  := (or .Site.Params.author.name  .Site.Params.authorName) -}}

{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>

    <title>
      {{- if eq .Title .Site.Title -}}
        {{ .Site.Title }}
      {{- else -}}
        {{ with .Title }}{{ . }} on {{ end }}{{ .Site.Title }}
      {{- end -}}
    </title>

    <link>{{ .Permalink }}</link>

    <description>
      Recent content
      {{ if ne .Title .Site.Title }}
        {{ with .Title }} in {{ . }} {{ end }}
      {{ end }}
      on {{ .Site.Title }}
    </description>

    <generator>Hugo -- gohugo.io</generator>

    {{- with .Site.Language.Locale }}
      <language>{{ . }}</language>
    {{- end }}

    {{- with $aemail }}
      <managingEditor>{{ . }}{{ with $aname }} ({{ . }}){{ end }}</managingEditor>
      <webMaster>{{ . }}{{ with $aname }} ({{ . }}){{ end }}</webMaster>
    {{- end }}

    {{- with .Site.Copyright }}
      <copyright>{{ . }}</copyright>
    {{- end }}

    {{- if not .Date.IsZero }}
      <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" }}</lastBuildDate>
    {{- end }}

    {{- with .OutputFormats.Get "RSS" }}
      <atom:link href="{{ .Permalink }}" rel="self" type="{{ .MediaType }}" />
    {{- end }}

    {{- range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <guid>{{ .Permalink }}</guid>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" }}</pubDate>

      {{- with $aemail }}
        <author>{{ . }}{{ with $aname }} ({{ . }}){{ end }}</author>
      {{- end }}

      {{- if eq .Site.Params.rssFullContent true }}
        <description>{{ .Content | html }}</description>
      {{- else if .Description }}
        <description>{{ .Description }}</description>
      {{- else }}
        <description>{{ .Summary | html }}</description>
      {{- end }}

    </item>
    {{- end }}

  </channel>
</rss>
